# V3 artwork v7

Built-in ImageGen precise-object edit based on `artwork-v6.png`.

## Intent

- Keep the windshield map, upper-left route direction, mapped wildfire region, and real fire intact.
- Make the vehicle location explicit with one filled white endpoint where the projected route begins nearest the car.
- Bake the warning card into the projected map area, not into the seat/cockpit and not as an HTML overlay.

## Card copy

```text
WATCH
Wind + route access
Recheck before the pass.
```

The card is a compact warm-ivory map annotation placed on the lower-left mapped landscape above the dashboard. `WATCH` uses safety orange; the remaining copy uses dark neutral text. No gradients, glass, shadow, extra icons, or duplicate interface elements.
