# V3 — Route-adjacent wildfire zone

Built with the built-in ImageGen editor from `artwork-v3.png`, using the original `artwork.png` only as reference for its mapped wildfire-zone notation.

## Required correction

- Keep the entire large projected route matte white; the road itself is not burning.
- Retain exactly one white route node at the mapped point nearest the fire.
- Place one irregular wildfire polygon to the right of that point, over the visibly burning right-side slope.
- Use a dashed off-white perimeter, sparse translucent diagonal hatching and one small safety-orange flame icon.
- Keep the real flames and smoke visible through the mapped overlay.
- No arrows, orange route, endpoint dots, alternate route, Plan B, final recommendation, UI text, gradient, logo or watermark.

The raster stays label-free. The adjacent HTML annotation names the mapped area explicitly.
