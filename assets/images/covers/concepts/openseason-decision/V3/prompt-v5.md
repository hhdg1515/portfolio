# V3 — Separated live fire and projected fire zone

Built with the built-in ImageGen editor from `artwork-v4.png`.

## Required correction

- Keep the real fire and smoke on the lower-right slope as the driver's visible condition.
- Move the projected wildfire polygon into the upper-right map layer so it does not overlap any real flame or smoke.
- Move the only route marker upward and turn it into one compact directional locator oriented toward the upper-right mapped zone.
- Keep the entire large route white.
- Preserve one dashed, translucent hatched fire zone with one safety-orange flame icon.
- No additional nodes, endpoint dots, wind arrows, orange road segment, alternate route, Plan B, final recommendation, UI text, gradient, logo or watermark.

The adjacent HTML annotation is positioned over the lower dark cockpit area so it does not cover either fire representation.
